{
  "name": "mim-cli",
  "version": "1.0.0",
  "private": true,
  "description": "Mim - Persistent Memory for Claude Code",
  "scripts": {
    "build": "npx esbuild src/mim.ts --bundle --platform=node --outfile=dist/mim.cjs --format=cjs",
    "clean": "rm -rf dist/"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.9.2"
  }
}
